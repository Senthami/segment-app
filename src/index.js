import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import SegmentPage from './containers/SegmentPage';

ReactDOM.render(
  <React.StrictMode>
    <SegmentPage />
  </React.StrictMode>,
  document.getElementById('root')
);
